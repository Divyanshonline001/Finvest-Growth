services:
  # 1. Backend API Web Service
  - type: web
    name: finvest-backend
    runtime: node
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: TOKEN_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false

  # 2. Frontend Landing Page (Static Site)
  - type: web
    name: finvest-frontend
    runtime: static
    plan: free
    region: oregon
    rootDir: frontend
    buildCommand: CI=false npm install && CI=false npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: finvest-backend
          property: host
      - key: REACT_APP_DASHBOARD_URL
        fromService:
          type: web
          name: finvest-dashboard
          property: host

  # 3. Trading Dashboard (Static Site)
  - type: web
    name: finvest-dashboard
    runtime: static
    plan: free
    region: oregon
    rootDir: dashboard
    buildCommand: CI=false npm install && CI=false npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: finvest-backend
          property: host
      - key: REACT_APP_FRONTEND_URL
        fromService:
          type: web
          name: finvest-frontend
          property: host
